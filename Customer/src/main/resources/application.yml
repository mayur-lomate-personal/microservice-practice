server:
  port: 8090
spring:
  application:
    name: customer
  datasource:
    username: 'mayur'
    url: jdbc:postgresql://localhost:5432/customer
    password: 'mayur123'
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true