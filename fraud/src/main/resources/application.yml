server:
  port: 8091
spring:
  application:
    name: fraud
  datasource:
    username: 'mayur'
    url: jdbc:postgresql://localhost:5432/fraud
    password: 'mayur123'
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true